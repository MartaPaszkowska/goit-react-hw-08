import styles from "./HomePage.module.css";

const HomePage = () => {
	return (
		<div className={styles.bg}>
			<p className={styles.title}>Your Private Phone Book</p>
		</div>
	);
};

export default HomePage;
